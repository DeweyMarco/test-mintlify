---
title: Changelog
description: Latest updates and changes to the Quotes API.
---

<Update label="2026-04-14" description="Week of April 14, 2026" tags={["New", "Update"]}>
  ## New: LLM-ready documentation

  The Quotes API docs now include [`llms.txt`](/llms.txt) and [`llms-full.txt`](/llms-full.txt) — plain-text summaries of the API designed for AI tools and large language models. Use `llms.txt` for a quick overview or `llms-full.txt` when you need full request and response details. See the [introduction](/introduction) page for usage examples.

  ## Update: Simplified API surface

  The versioned Trading API (v1, v2, v3), Broker API, and regional navigation have been retired. All four endpoints — [Get random quote](/api-reference/get-random-quote), [List quotes](/api-reference/list-quotes), [Get quote by ID](/api-reference/get-quote-by-id), and [List categories](/api-reference/list-categories) — now live under a single API with no version or region prefixes. If you were using Trading API or Broker API endpoints, switch to the equivalent Quotes API paths — request and response formats remain the same.

  ## Update: Willow theme

  The docs site now uses the Willow theme with improved dark mode contrast and a cleaner visual style.
</Update>

<Update label="2026-04-13" description="Latest updates" tags={["Update"]}>
  ## Broker API request examples and error responses

  The Broker API reference pages now include request examples and error handling details. Each endpoint shows a sample `curl` request and documents error responses, so you can integrate faster without guesswork.

  - List brokers — now includes a request example
  - Get broker — now includes a request example and `404` error response

  ## Visual refresh

  The docs have a new look. We switched to a cleaner theme with the Geist font, updated colors, breadcrumb navigation, and refined code block styling for better readability.
</Update>

<Update label="2026-04-13" description="Week of April 13, 2026" tags={["New", "Update"]}>
  ## New: Trading API

  You can now access quotes through three versioned Trading API products, each scoped to specific endpoints:

  - **Trading API v1** — Get random quote and List quotes
  - **Trading API v2** — Get quote by ID
  - **Trading API v3** — List categories

  Pick the version that includes the endpoints you need.

  ## New: Broker API

  A new Broker API lets you discover and retrieve broker details in the US region. Two endpoints are available:

  - List brokers — Returns all brokers in your region
  - Get broker — Retrieve details for a specific broker by ID

  Both endpoints include request examples and document error responses.

  ## New: Interactive API playground

  Every API reference page now includes a built-in playground. Adjust parameters and see live responses without leaving the docs.

  ## New: Regional navigation

  The docs are now organized by region. Use the sidebar dropdown to switch between API products — Quotes API, Trading API versions, and Broker API.

  ## Update: Production base URL coming soon

  A hosted production base URL will be available shortly, so you can integrate without running the server locally. Check the [introduction](/introduction) page for updates.

  ## Update: Revised API description

  The Quotes API description now reads "A lightweight REST API for fetching quotes by category," giving you a clearer picture of what the API does at a glance.

  ## Update: Visual refresh

  The docs have a new look with a cleaner theme, the Geist font, breadcrumb navigation, and refined code block styling.
</Update>

<Update label="2026-04-02" description="Week of April 2, 2026" tags={["New", "Breaking"]}>
  ## New "marco original" category

  A new `marco original` category is now available, featuring an original quote by Marco Dewey. Filter by it using the [Get random quote](/api-reference/get-random-quote) or [List quotes](/api-reference/list-quotes) endpoints.

  ## Humor category removed

  The `humor` category has been fully removed. It no longer appears in the schema or returns any results.

  The active categories are now: `marco original`, `motivation`, `philosophy`, `technology`, and `wisdom`. See the [List categories](/api-reference/list-categories) endpoint for the current list.
</Update>
