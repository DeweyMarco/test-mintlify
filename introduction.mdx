---
title: Introduction
description: A lightweight REST API for fetching quotes by category.
public: true
---

## Overview

The Quotes API is a simple REST API that serves a curated collection of quotes across categories including **motivation**, **wisdom**, **philosophy**, **technology**, and **marco original**.

You can integrate it easily — no authentication required, JSON responses, and predictable error shapes.

## Base URL

A production base URL is coming soon. In the meantime, you can use the local development server:

```text
http://localhost:3000
```

## Available endpoints

| Method | Path | Description |
| --- | --- | --- |
| `GET` | `/quotes` | List all quotes, with optional filters |
| `GET` | `/quotes/random` | Get a random quote |
| `GET` | `/quotes/{id}` | Get a specific quote by ID |
| `GET` | `/categories` | List available categories |

## Response format

All successful responses return JSON. Errors follow a consistent shape:

```json
{
  "error": "invalid_category",
  "message": "Category 'foo' does not exist. Valid categories: marco original, motivation, philosophy, technology, wisdom"
}
```

## Categories

You can filter quotes by category. The active categories are:

| Category | Description |
| --- | --- |
| `marco original` | Original quotes by Marco |
| `motivation` | Motivational and inspirational quotes |
| `philosophy` | Philosophical quotes |
| `technology` | Technology-related quotes |
| `wisdom` | Quotes about wisdom and life lessons |

## No authentication required

This API is open — no API keys or tokens needed.

## LLM-ready documentation

If you're building AI-powered integrations, two plain-text files provide structured summaries of the API that large language models can consume directly:

| File | Description |
| --- | --- |
| [`llms.txt`](/llms.txt) | Concise site map with one-line descriptions of every docs page and the OpenAPI spec |
| [`llms-full.txt`](/llms-full.txt) | Complete API context including endpoints, categories, example requests, error format, and project notes |

Point your LLM at either file so it can understand the Quotes API without parsing the full docs site. Use `llms.txt` for a quick overview or `llms-full.txt` when the model needs full request/response details.

```text
# Fetch the concise summary
curl https://your-docs-domain/llms.txt

# Fetch the full context
curl https://your-docs-domain/llms-full.txt
```
